import { StyleSheet } from "react-native";

export default StyleSheet.create({
    header: {
        backgroundColor: '#f16820'
    },
    icon: {
        color: 'white', 
    },
    image: {
        width: 64, height: 24, 
        resizeMode: 'stretch', 
        tintColor: 'white',
    },
});